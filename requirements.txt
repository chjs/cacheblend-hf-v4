# Locked versions for Mac venv (CPU) ↔ Pod GPU (CUDA 12.4) parity.
# Phase 0의 scripts/diff_env.sh가 이 파일과 실제 설치 버전을 비교.
#
# Install order on Mac:
#   pip install torch==2.4.1
#   pip install -r requirements.txt
#   pip install -e .
#
# On Pod (torch 2.4.1+cu124 already preinstalled):
#   grep -v -E '^torch(\s|=|$)' requirements.txt > /tmp/reqs-no-torch.txt
#   pip install -r /tmp/reqs-no-torch.txt
#   pip install -e .

# --- Core (locked exactly to v3 final versions) ---
torch==2.4.1
transformers==4.49.0
accelerate==1.13.0
huggingface-hub==0.36.2
tokenizers==0.21.4
safetensors==0.7.0

# --- Datasets ---
datasets==4.8.5

# --- RAG ---
sentence-transformers>=3.0,<5.0

# --- Metrics ---
rouge-score>=0.1.2
scipy>=1.11

# --- Phase 7 only (70B 8-bit) — install on demand ---
# bitsandbytes>=0.43

# --- Plots & numerics ---
numpy>=2.0,<3.0
matplotlib>=3.8
pandas>=2.0

# --- Email reporting ---
python-dotenv>=1.0

# --- Dev ---
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5
